import type { Analysis } from "../types";
export default function RiskCard({ analysis }: { analysis: Analysis }) { const tone = analysis.risk.level.toLowerCase(); return <section className={`risk-card ${tone}`}><p className="eyebrow">Significant rainfall risk</p><div className="risk-content"><div><strong>{analysis.risk.level}</strong><span>Risk level from deterministic assessment</span></div><div className="score"><b>{analysis.risk.score.toFixed(1)}</b><span>/ 100</span></div></div><div className="probability"><span>Probability of ≥20 mm rainfall in the next 24 hours</span><b>{Math.round(analysis.ml_prediction.probability * 100)}%</b></div></section>; }
