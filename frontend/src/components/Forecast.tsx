import type { HourlyWeather } from "../types";
const formatHour = (time: string) => new Intl.DateTimeFormat(undefined, { hour: "numeric", hour12: true }).format(new Date(time));
export default function Forecast({ hours }: { hours: HourlyWeather[] }) { return <section className="panel"><div className="section-heading"><div><p className="eyebrow">Outlook</p><h2>Next 24 hours</h2></div><span className="muted">Scroll to explore →</span></div><div className="forecast-scroll">{hours.map(hour => <article className="hour-card" key={hour.time}><b>{formatHour(hour.time)}</b><span>{hour.temperature_c?.toFixed(0) ?? "—"}°C</span><small>Rain {hour.precipitation_mm?.toFixed(1) ?? "—"} mm</small><small>Chance {hour.precipitation_probability_percent?.toFixed(0) ?? "—"}%</small></article>)}</div></section>; }
