import type { Analysis } from "../types";
export default function Explanation({ analysis }: { analysis: Analysis }) { const explanation = analysis.explanation; if (!explanation) return null; return <section className="panel explanation"><div className="section-heading"><div><p className="eyebrow">Verified explanation</p><h2>Why is the risk at this level?</h2></div><span className="source-pill">{explanation.source === "template" ? "Verified evidence" : "Provider explanation"}</span></div><p className="summary">{explanation.summary}</p><div className="explanation-grid"><List title="Why this risk" values={explanation.why_this_risk} /><List title="Key factors" values={explanation.key_factors} /><List title="What to watch" values={explanation.what_to_watch} /></div><p className="disclaimer">{explanation.disclaimer}</p></section>; }
function List({ title, values }: { title: string; values: string[] }) { return <div><h3>{title}</h3><ul>{values.map(value => <li key={value}>{value}</li>)}</ul></div>; }
