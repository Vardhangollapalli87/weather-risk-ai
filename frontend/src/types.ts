export type Location = { id: number | null; name: string; country: string | null; admin1: string | null; latitude: number; longitude: number; timezone: string | null };
export type HourlyWeather = { time: string; temperature_c: number | null; precipitation_mm: number | null; precipitation_probability_percent: number | null; weather_code: number | null };
export type Analysis = { location_name: string | null; weather: { location: Location; timezone: string; current: { time: string; temperature_c: number | null; relative_humidity_percent: number | null; wind_speed_kmh: number | null; cloud_cover_percent: number | null }; hourly_forecast: HourlyWeather[]; rainfall: { recent_24h_mm: number; forecast_next_24h_mm: number }; provider: { name: string }; retrieved_at: string; freshness: { status: string; age_minutes: number } }; ml_prediction: { probability: number; target: string; model_version: string }; risk: { score: number; level: "Low" | "Moderate" | "High"; factors: { name: string; value: number; indicator: number; threshold: string }[] }; explanation: { summary: string; why_this_risk: string[]; key_factors: string[]; what_to_watch: string[]; disclaimer: string; source: string } | null; status: string };
